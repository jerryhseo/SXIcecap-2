import React, {useContext, useState, useEffect, useRef} from 'react';

const StructuredDataViewer = ({portletParameters}) => {
	return <h1>Structured Data Viewer</h1>;
};

export default StructuredDataViewer;
