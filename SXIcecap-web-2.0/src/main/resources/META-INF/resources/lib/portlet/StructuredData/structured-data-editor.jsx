import React, {useContext, useState, useEffect, useRef} from 'react';

const StructuredDataEditor = ({portletParameters}) => {
	return <h1>Structured Data Editor</h1>;
};
export default StructuredDataEditor;
