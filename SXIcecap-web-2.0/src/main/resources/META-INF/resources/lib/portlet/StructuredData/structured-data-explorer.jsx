import React, {useContext, useState, useEffect, useRef} from 'react';

const StructuredDataExplorer = ({portletParameters}) => {
	return <h1>Structured Data Explorer</h1>;
};

export default StructuredDataExplorer;
